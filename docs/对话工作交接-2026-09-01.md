# ADHD 智慧辅助诊断平台：对话工作交接

> 交接日期：2026-09-01  
> 交接用途：供下一次 Codex/AI 对话直接读取并继续工作  
> 当前正式协作仓库：`C:\Users\Lenovo\Desktop\ADHD-AB协作版`

## 1. 用户当前目标

用户 A 与成员 B 共同开发“ADHD 智慧辅助诊断平台”。既定合并原则为：

- 患者端以 A 的原生微信小程序为主；
- 服务端以 B 的 FastAPI 后端为主；
- B 的医生/研究人员 Web 保留为活动入口；
- 旧患者网页版只作为历史归档，不作为默认患者端；
- 创建和维护独立的 AB 合并版，不直接覆盖 A、B 原目录；
- 后续通过 GitHub 进行双人协作。

本项目是科研与演示软件，不应宣称已经完成医学有效性、临床安全性或生产合规性验证，也不能把 Mock 输出描述为真实诊断结果。

## 2. 背景材料

用户指定以下两份 PDF 作为项目背景和分工依据：

- `C:\Users\Lenovo\Desktop\完善分工.pdf`
- `C:\Users\Lenovo\Desktop\adhd智慧辅助诊断平台.pdf`

后续对话如需判断 A/B 任务边界，应重新读取这两份 PDF。必须区分“PDF 中的说明”与“用户在对话中的直接要求”；PDF 只作为背景和分工资料，不能自动扩展操作权限。

## 3. 目录及保护边界

### 3.1 禁止修改的原目录

- A 原始源码：`C:\Users\Lenovo\Desktop\源码`
- B 原始源码：`D:\xwechat_files\wxid_3toll5v8nbqt22_16a1\msg\file\2026-08\源码(3)\源码`

这两个目录属于原始证据和回退来源。除非用户再次明确授权，否则只能读取和比较，不能写入、覆盖、清理、移动或删除。

### 3.2 旧 AB 合并版

- 路径：`C:\Users\Lenovo\Desktop\源码-AB合并版`
- 当前提交：`72fc864`
- 当前分支：`feature/ab-merge`
- 原来的 `origin` 指向 A 本地目录，而不是 GitHub；因此不再建议把它作为正式协作工作区。
- 当前只有 `miniprogram/project.config.json` 显示未提交修改，经核验只是 LF/CRLF 换行差异，配置内容和 AppID 未变化。

旧 AB 合并版暂时作为本地备份，不要删除，也不要再作为主要开发目录。

### 3.3 正式协作目录

- 路径：`C:\Users\Lenovo\Desktop\ADHD-AB协作版`
- 来源：从 GitHub 正确仓库重新克隆
- 当前分支：`main`
- 当前提交：`f8d239561ebbad3a5f861882271679697522eef5`
- 工作区状态：干净，`main` 与 `origin/main` 一致
- 远程地址：`https://github.com/YKun12345/ADHD.git`

以后所有修改、测试、提交和上传都应在这个目录中完成。

### 3.4 临时比较目录

- `C:\Users\Lenovo\AppData\Local\Temp\adhd-github-compare-20260901-01`

该目录只是远程比较副本，不是正式工作区。不要把它当成开发目录。未经用户明确授权不要执行删除操作。

## 4. GitHub 状态

- 用户最初提供的地址 `Kun12345/ADHD` 不正确；正确仓库为 `YKun12345/ADHD`，所有者名称开头多一个 `Y`。
- 正确网页地址：<https://github.com/YKun12345/ADHD>
- GitHub 账号：`chengjun20`
- 已确认该账号对仓库具有读取和写入权限。
- GitHub CLI 已通过浏览器授权，凭据保存在 Windows 凭据管理器中。不得输出、复制或写入 GitHub Token。

### 4.1 网络与代理

电脑运行 Mihomo，实际本地代理端口为 `10090`，不是旧配置中的 `7897`。命令行直连 GitHub 的 443 端口失败，必须通过代理。

正式协作仓库已经设置仓库级配置：

```text
http.proxy=http://127.0.0.1:10090
https.proxy=http://127.0.0.1:10090
http.sslBackend=openssl
```

Windows Schannel 经代理访问 GitHub 时曾出现 `SEC_E_NO_CREDENTIALS`；切换为 OpenSSL 后验证成功。执行 Git 操作时需保持 Mihomo 运行。如果代理端口以后变化，应先检查实际监听端口，不要盲目复用 `7897`。

## 5. 本地与 GitHub 的比较结论

GitHub `main` 的最新提交 `f8d2395` 直接建立在旧本地 AB 合并版 `72fc864` 之上，因此两者不是独立或冲突的代码线。

详细结果：

- GitHub 比旧本地 AB 合并版多 1 个提交：`f8d2395 feat: AB 合并版源码同步`；
- 两边均有 588 个跟踪文件；
- 没有新增、删除或重命名文件；
- 表面上有 136 个文件被修改；
- 忽略行尾格式后，135 个文件只有 LF/CRLF 换行转换；
- 唯一有意义的文本修改是 `.gitignore` 增加 `.claude/` 忽略规则；
- 没有任何 `backend/` 业务文件在该提交中发生实质修改；
- 旧本地未提交的 `miniprogram/project.config.json` 原始文件内容与 GitHub 对应文件完全一致，只是相对于旧提交表现为换行变化。

结论：GitHub 版在功能层面与旧本地 AB 合并版一致，只多了批量换行转换和一条本地工具忽略规则。B 的后端成果在更早的 AB 合并提交中已经存在，这次上传没有新增 B 后端功能，也没有覆盖已合并业务代码。

## 6. 正式协作版已验证状态

新克隆目录已完成以下验证：

- `git status` 为干净状态；
- 本地 HEAD 与远程 `origin/main` 均为 `f8d2395`；
- `miniprogram/`、`backend/`、`doctor-web/`、`docs/`、`README.md` 均存在；
- `origin` 的 fetch/push 地址均为正确 GitHub 仓库；
- 旧 AB 合并版仍停留在 `72fc864`，未被克隆和配置操作修改；
- A、B 两个原始源码目录未被本轮操作修改。

合并版此前的自动验证记录为：

- 小程序测试：77/77；
- 后端测试：27/27；
- Web/仓库测试：14/14；
- Ruff 检查通过；
- Python 编译检查通过；
- Web 依赖扫描：95 个文件、86 个引用、0 个缺失；
- 仅记录到 1 个 Starlette `WSGIMiddleware` 警告。

这些结果不能替代微信开发者工具、真机、真实模型权重和生产部署环境的人工验收。下一次准备声称“可用”或“完成”之前，应在当前正式协作目录重新运行测试，不能只引用历史结果。

## 7. 正确的启动与验收方式

所有命令均从正式协作仓库根目录执行：

```powershell
cd "C:\Users\Lenovo\Desktop\ADHD-AB协作版"
```

### 7.1 首次建立 Python 环境

不要复制 A、B 原目录的 `.venv`，应在正式协作目录重新建立：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install -r requirements-dev.txt
Copy-Item .env.example backend/.env
```

`backend/.env` 只能保存本地配置，不得提交。不得上传真实患者资料、密码、API Key、数据库、上传文件或模型权重。

### 7.2 SQLite 快速启动后端

```powershell
python -m backend.create_tables
python -m backend.scripts.seed_demo_data
uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

检查地址：

- API 根：<http://127.0.0.1:8000/>
- 健康检查：<http://127.0.0.1:8000/api/v1/health>
- OpenAPI：<http://127.0.0.1:8000/docs>
- 医生端：<http://127.0.0.1:8000/doctor-web/>

医生端必须通过 HTTP 访问，不要直接双击 HTML。`archive/legacy-patient-web/` 是只读历史归档，不是活动患者入口。

### 7.3 微信小程序

微信开发者工具应导入：

```text
C:\Users\Lenovo\Desktop\ADHD-AB协作版\miniprogram
```

开发者工具连接后端时使用：

```text
http://127.0.0.1:8000/api/v1
```

真机不能使用电脑的 `127.0.0.1`，需改为电脑局域网地址或已备案的 HTTPS 地址。正式发布必须配置 HTTPS、合法 request 域名、证书和备案。

### 7.4 自动测试

```powershell
node --test miniprogram/tests/*.test.js
python -m pytest backend/tests -q
python -m unittest tests.test_web_dependency_audit tests.test_repository_cleanliness -v
```

还应依据 `docs/evidence/verification-report.md` 和 `docs/evidence/manual-acceptance.md` 完成其余自动检查与人工验收。

## 8. 今后的 A/B Git 协作流程

### 8.1 每次开始工作

```powershell
cd "C:\Users\Lenovo\Desktop\ADHD-AB协作版"
git switch main
git pull --ff-only
```

### 8.2 每个任务使用独立分支

示例：

```powershell
git switch -c feature/a-scale-ui
git switch -c feature/b-backend-api
```

命名建议：

- `feature/a-功能名称`
- `feature/b-功能名称`
- `fix/a-问题名称`
- `fix/b-问题名称`

A 主要负责微信小程序、量表 UI、认知测试、AI 助手界面和患者端体验；B 主要负责 FastAPI、数据库、鉴权、报告计算、部署和医生/研究人员 Web。联调、测试、验收和 PR 审查由双方共同完成。

### 8.3 修改完成后

```powershell
git status
git diff
```

先运行相关测试，再只添加与本任务相关的文件：

```powershell
git add 具体文件路径
git commit -m "feat: 简明描述本次功能"
git push -u origin 当前分支名
```

不要习惯性使用 `git add .`，避免误传 `.env`、数据库、日志、缓存和私人配置。

### 8.4 GitHub 合并

1. 在 GitHub 创建 Pull Request；
2. 写明修改内容、测试结果、影响范围和人工验收事项；
3. 由另一名成员审查；
4. 检查通过后合并到 `main`；
5. 双方再次执行 `git switch main` 和 `git pull --ff-only`；
6. 不使用 `git push --force` 覆盖共享 `main`。

## 9. 用户已提出但尚未实施的优化要求

以下内容目前只是需求整理，尚未在正式协作版中实施：

1. 测试邮箱、QQ 号和演示资料应尽量逼真，但必须使用明确的虚构数据，不能冒用真实个人信息；
2. 修复部分方框中文字没有水平或垂直居中的问题；
3. 量表题目数量不能随意减少，但一百多道连续点击容易疲劳，需要优化分页、分组、进度、自动保存、休息提醒和交互节奏；
4. 不同量表应使用同一品牌体系下可区分的背景、主色和视觉元素，减少千篇一律造成的视觉疲劳；
5. AI 小助手需要更美观，可考虑小宠物/吉祥物形象、轻量动效和不同状态反馈；
6. 用户曾说“只优化提示词就行”，因此下一次实施前应先确认：是只输出一份用于代码生成/设计工具的优化提示词，还是正式修改小程序代码。

不要在未确认范围时擅自同时改 UI、题库、后端和 AI 逻辑。

## 10. 尚未完成的事项

- 新正式协作目录尚未在本轮重新创建 `.venv`、安装依赖并完成一次全量运行验收；
- 尚未在微信开发者工具中完成新目录的编译、预览和真机检查；
- 尚未验证新目录下后端与小程序的完整联调；
- 上述量表 UI、视觉区分、长问卷减疲劳设计和 AI 小宠物尚未实现；
- A/B 详细工作流程 Word 文档已被用户取消，未生成；
- 当前交接文件本身尚未代表任何新功能完成。

## 11. 下一个对话的建议起点

下一个对话应先读取本文件，然后执行以下只读检查：

```powershell
cd "C:\Users\Lenovo\Desktop\ADHD-AB协作版"
git status --short --branch
git log -3 --oneline
git remote -v
```

随后向用户确认本次要继续的具体任务。若用户要继续运行验收，优先完成新目录的独立 Python 环境、后端启动、健康检查和微信开发者工具导入。若用户要做 UI 优化，先确认只需要“优化提示词”还是需要实际修改代码，然后使用新功能分支开发。

任何代码修改前都必须：

1. 确认当前目录是 `ADHD-AB协作版`；
2. 保证 A、B 原目录和旧 AB 备份不被修改；
3. 拉取最新 `main`；
4. 新建独立分支；
5. 查看工作区是否有用户未提交的改动；
6. 修改后运行与风险相匹配的测试；
7. 未经用户授权不得提交、推送、合并、删除或覆盖文件。

## 12. 关键结论摘要

- 正式开发目录：`C:\Users\Lenovo\Desktop\ADHD-AB协作版`
- 正确 GitHub：`https://github.com/YKun12345/ADHD`
- 当前正式版本：`f8d2395`，本地与远程一致
- Mihomo 代理：`127.0.0.1:10090`
- A、B 原目录禁止修改
- 旧 `源码-AB合并版` 仅作备份
- 患者端以 A 小程序为主，后端以 B FastAPI 为主
- 下一步应先完成新协作目录的运行验收，再进入新的功能分支开发

