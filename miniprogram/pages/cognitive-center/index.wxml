<ai-copilot page-key="cognitive-center" />
<ui-nav title="认知测试中心" />
<view class="cognitive-center-page ai-copilot-safe-space">
  <view class="center-content">
    <view class="page-heading">
      <text class="page-kicker">你好，{{patientName}}</text>
      <text class="page-title">完成七项认知任务</text>
      <text class="page-subtitle">
        成人与儿童参数自动适配，可完整评估或单项完成
      </text>
    </view>

    <view class="summary-card glass-surface">
      <view class="summary-header">
        <view class="summary-copy">
          <text class="summary-title">认知任务进度</text>
          <text class="summary-description">{{summaryText}}</text>
        </view>
        <text class="summary-count">{{completedCount}} / {{totalCount}}</text>
      </view>
      <view class="summary-progress-track">
        <view
          class="summary-progress-value"
          style="{{'width: ' + progressPercent + '%;'}}"
        ></view>
      </view>
      <view class="completion-banner" wx:if="{{allCompleted}}">
        <view class="completion-banner__mark" aria-hidden="true"></view>
        <text>七项任务已完成，最近结果已保存在本机</text>
      </view>
    </view>

    <view class="battery-card glass-surface">
      <view class="battery-copy">
        <text class="battery-title">完整认知评估</text>
        <text class="battery-description">按推荐顺序完成七项，项目之间可退出后继续</text>
        <text class="battery-time">预计剩余 {{remainingMinutes}} 分钟</text>
      </view>
      <button class="battery-button ui-button ui-button--primary" bindtap="startOrResumeBattery">{{batteryActionText}}</button>
    </view>

    <view class="test-list">
      <view
        class="test-card {{item.completed ? 'test-card--completed' : ''}}"
        wx:for="{{cards}}"
        wx:key="id"
        data-id="{{item.id}}"
        hover-class="ui-clickable--pressed"
        aria-role="button"
        bindtap="handleTestTap"
      >
        <view class="test-icon">
          <ui-icon
            name="{{item.iconName}}"
            shape="{{item.iconShape}}"
            decorative="{{true}}"
          />
        </view>
        <view class="test-copy">
          <view class="test-title-row">
            <text class="test-title">{{item.title}}</text>
            <text class="completed-badge" wx:if="{{item.completed}}">已完成</text>
          </view>
          <text class="test-description">{{item.description}}</text>
          <text class="test-metric">{{item.primaryMetric}}</text>
        </view>
        <view class="test-action">
          <text>{{item.statusLabel}}</text>
          <view class="test-arrow" aria-hidden="true"></view>
        </view>
      </view>
    </view>

    <view class="privacy-note">
      <text class="privacy-title">本地结果说明</text>
      <text class="privacy-text">
        中心仅保存最近一次客观指标，不保存姓名、邮箱或登录凭据。
      </text>
    </view>

    <text class="medical-tip">
      本中心仅汇总任务完成情况和客观指标，不替代专业医生诊断
    </text>
  </view>
</view>
